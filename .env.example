# Rename to .env and fill in your values
APP_USERNAME=admin
APP_PASSWORD=change_me_strong_password
SECRET_KEY=generate_random_secret_key_here

# Test Mode:
# 0 = real scraping (ScraperAPI + Playwright fallback) - SLOW ~60s, pode falhar
# 1 = use fixed URLs (URLs expiram rapidamente, NÃO RECOMENDADO)
# 2 = mock data (RECOMENDADO para testes) - INSTANT, sempre funciona, preços variam
TEST_MODE_LOCAL=2

# Optional: Fixed test URLs for Mac development (when TEST_MODE_LOCAL=1)
# NOTA: URLs com s= e b= expiram rapidamente, não são confiáveis
TEST_FARO_URL=
TEST_ALBUFEIRA_URL=

# Optional scraping proxy (recommended for production)
SCRAPER_SERVICE=
SCRAPER_API_KEY=
